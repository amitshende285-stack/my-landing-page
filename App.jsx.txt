import React from 'react';
import { ChevronRight, Star, Shield, Zap, Layout, ArrowRight } from 'lucide-react';

const LandingPage = () => {
  return (
    <div className="min-h-screen bg-white font-sans text-slate-900">
      <nav className="flex items-center justify-between px-8 py-6 max-w-7xl mx-auto">
        <div className="flex items-center gap-2">
          <div className="w-10 h-10 bg-blue-600 rounded-xl flex items-center justify-center">
            <Zap className="text-white" size={24} />
          </div>
          <span className="text-xl font-bold tracking-tight">NexusStream</span>
        </div>
        <div className="hidden md:flex items-center gap-8 text-sm font-medium text-slate-600">
          <a href="#" className="hover:text-blue-600">Product</a>
          <a href="#" className="hover:text-blue-600">Solutions</a>
          <a href="#" className="hover:text-blue-600">Pricing</a>
        </div>
        <button className="px-5 py-2.5 bg-blue-600 text-white text-sm font-medium rounded-full hover:bg-blue-700 shadow-lg shadow-blue-200">
          Get Started
        </button>
      </nav>

      <section className="px-8 pt-20 pb-32 max-w-7xl mx-auto text-center">
        <h1 className="text-5xl md:text-7xl font-extrabold mb-8 tracking-tight leading-[1.1]">
          Scale your business <br />
          <span className="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-indigo-600">without the complexity.</span>
        </h1>
        <p className="max-w-2xl mx-auto text-lg md:text-xl text-slate-600 mb-10">
          The all-in-one platform to manage workflows, automate customer interactions, and track growth analytics in real-time.
        </p>
        <div className="flex flex-col sm:flex-row items-center justify-center gap-4">
          <button className="px-8 py-4 bg-slate-900 text-white font-semibold rounded-2xl hover:bg-slate-800 flex items-center gap-2">
            Start free trial <ArrowRight size={20} />
          </button>
        </div>
      </section>

      <section className="bg-slate-50 py-32 px-8">
        <div className="max-w-7xl mx-auto grid md:grid-cols-3 gap-8">
          <FeatureCard title="Enterprise Security" desc="Bank-grade encryption to keep your data safe." />
          <FeatureCard title="Visual Dashboard" desc="Intuitive interface for complex data visualization." />
          <FeatureCard title="Instant Deployment" desc="Go live in minutes with our global network." />
        </div>
      </section>
    </div>
  );
};

const FeatureCard = ({ title, desc }) => (
  <div className="p-8 bg-white rounded-3xl border border-slate-100 hover:shadow-xl transition-all">
    <h3 className="text-xl font-bold mb-3">{title}</h3>
    <p className="text-slate-600">{desc}</p>
  </div>
);

export default LandingPage;